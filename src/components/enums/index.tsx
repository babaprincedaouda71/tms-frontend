export enum ButtonType {
  submit = "submit",
  default = "default",
}

export enum COLORS {
  primary = "primary",
  secondary = "secondary",
  lightBlack = "lightBlack",
  gray = "gray",
  default = "default",
}

export enum Sort {
  asc = "asc",
  desc = "desc",
}

export enum RequestDecesion {
  APPROVE = "APPROVE",
  DECLINE = "DECLINE",
}

export enum SETTLEMENT_STATUS {
  upcoming = "Upcoming",
  finalized = "FINALIZED",
  settled = "SETTLED",
  overdue = "Overdue",
  paid = "Paid",
}
