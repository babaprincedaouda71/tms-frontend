export enum ButtonType {
  submit = "submit",
  default = "default",
}

export enum COLORS {
  primary = "primary",
  secondary = "secondary",
  lightBlack = "lightBlack",
  gray = "gray",
  default = "default",
}
